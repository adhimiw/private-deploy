# ============================================
# VARMAN CONSTRUCTIONS - Environment Variables
# ============================================
# IMPORTANT: Copy this file to .env and update values
# NEVER commit .env file to version control!

# Server Configuration
NODE_ENV=production
PORT=3001

# Security - JWT Secret (MUST be at least 32 characters, use a random string)
# Generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-chars-here

# CORS - Allowed Origins (comma-separated for multiple)
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=https://varmanconstructions.in,http://www.varmanconstructions.in

# Admin Credentials (change these on first login!)
# Default: admin / varman@2024
# IMPORTANT: Change the password immediately after first login

# Email Configuration (optional - for contact form notifications)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
ADMIN_EMAIL=info@varmanconstructions.in
